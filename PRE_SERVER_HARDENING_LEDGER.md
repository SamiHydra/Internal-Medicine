# Pre-server hardening ledger

Branch `hardening/pre-server` (from release candidate `b07b32c`, branch
`fix/mobile-touch-targets`). Status date: 2026-09-09. Every row names the test
that guards it and how the runtime behaviour was observed; "Runtime
verification" is the environment where it was actually exercised (LOCAL =
this workstation's isolated gate, PARITY = the Docker Linux/nginx/PHP-FPM
8.3/MariaDB 11.4 stack, UNIT = automated tests only).

Statuses: TODO, IN PROGRESS, FIXED, VERIFIED, ACCEPTED-MONITOR, BLOCKED, NOT APPLICABLE.

| ID | Area | Finding | Action | Tests | Runtime verification | Status |
|---|---|---|---|---|---|---|
| H-001 | Offline sync | No stale-write detection: a queued or two-device save silently overwrote a newer server copy (last write wins) | `POST/PUT /api/reports` accept `expectedUpdatedAt` (the loaded `updatedAt`, or `null` for "no report yet"); the service checks it under the row lock and answers 409 with the server's row and cell values (`ReportConflictException`); callers that omit the key keep last-write-wins | `ReportWorkflowTest` (5 new: matching revision, stale 409, exists 409, stale on locked, no-revision compatibility); `offline-sync.spec.ts` C | LOCAL gate, PARITY (`offline-sync` C through the production bundle in the 2026-09-07 parity run) | VERIFIED |
| H-002 | Offline sync | A refused queued save was deleted after 5 attempts with only a toast ("discarded, please re-enter") | Refused saves are parked (`status: conflict`) with the server's answer; a review panel shows every differing cell (mine / server), with apply, keep-server and copy actions; nothing is deleted without the user | `report-save-queue.conflict.test.ts` (6), `conflict-rows.test.ts` (4); `offline-sync.spec.ts` B, C | LOCAL gate | VERIFIED |
| H-003 | Offline sync | An expired session during replay counted as a failed attempt; five expiries would discard the work | 401/419 stop the replay without counting; the queue survives sign-out and replays after the same user's next sign-in | `report-save-queue.conflict.test.ts` (classification); `offline-sync.spec.ts` E | LOCAL gate | VERIFIED |
| H-004 | Offline sync | Every open tab replayed the whole queue on reconnect (duplicate POSTs) | Replay runs under a Web Lock (`stpaul:offline-report-sync`), one tab at a time; per-tab guard kept as fallback | `offline-sync.spec.ts` G (3 tabs, 3 saves, order preserved, each saved once) | LOCAL gate | VERIFIED |
| H-005 | Offline sync | Browser restart and flapping network were untested | Scenarios A, D, F added (persistent profile reopened with the API unreachable; four online/offline flips during saves) | `offline-sync.spec.ts` A, D, F (15 of 15 incl. setup) | LOCAL gate | VERIFIED |
| H-006 | Offline sync | No written conflict model | `docs/OFFLINE_SYNC_MODEL.md` (queue lifecycle, retry, conflict, stale-write, auth expiry, locks, user flow, permanent failure) | n/a | n/a | VERIFIED |
| H-007 | Parity reproducibility | A Windows checkout with `core.autocrlf=true` rewrote the shell scripts with CRLF; the parity `app` container crash-looped (`/usr/bin/env: bash\r`) and `deploy.sh` would break the same way | `.gitattributes` pins LF for `*.sh`, Dockerfile, `deploy/**`, `docker/**`, `.env.docker`; the Docker images strip CR from their entrypoints defensively | stack boots from a fresh checkout | PARITY (stack healthy after the fix; rebuilt three times since) | VERIFIED |
| H-008 | Observability | No error reporting hooks, no 5xx/slow-request/failed-job counters, no client error capture | `ErrorReporter` (log + hourly counters + optional `OBSERVABILITY_WEBHOOK_URL`), `Redactor`, `RecordRequestTiming` middleware (`Server-Timing`, slow-request log), `Queue::failing` listener, `POST /api/client-errors` (public, throttled, strict schema), browser reporter (uncaught errors, rejections, API 5xx, offline-sync give-up), `RouteErrorBoundary` | `ObservabilityTest` (12), `error-reporter.test.ts` (5), `route-error-boundary.test.tsx` (2) | UNIT; PARITY (health snapshot read through nginx) | VERIFIED |
| H-009 | Observability | Sensitive data could ride along in error reports | Redaction of credentials, tokens, cookies, authorization, query strings, e-mail local parts; request bodies, report values, evaluation content and uploads are never collected; documented rules in `docs/OBSERVABILITY.md` | `ObservabilityTest` (client report, exception, failed job, redactor) | UNIT | VERIFIED |
| H-010 | System health | Readiness existed only as a console command | `GET /api/admin/system-health` (permission `system.health`, Maintenance only) and `/admin/system-health` page: release, database latency and pending migrations, queue depth/age, failed jobs, scheduler age, backup ages, disk, storage writability, transport names, error counters, readiness table; no secrets | `ObservabilityTest::test_system_health_is_maintenance_only_and_never_leaks_secrets` (401/403/403/200, no `APP_KEY`/`DB_PASSWORD`/`base64:`) | PARITY (smoke check 7) | VERIFIED |
| H-011 | Release identification | The backend had no notion of the running release | `deploy.sh` writes `release.json` into the immutable release; the Docker images bake it (`RELEASE_SHA`); `Release` resolves env, then file, then `unknown`; every log line carries `release`; health snapshot and smoke check 2 report it | `ObservabilityTest` (release resolution, log context) | PARITY (`release` visible in the health snapshot and `laravel.log`) | VERIFIED |
| H-012 | Data integrity | Deleting a ward or section referenced by placements, teaching sessions, evaluations or transfer requests answered a raw 500 | `AcademicStructureController::destroyWard/destroySection` refuse with 422 and the existing "deactivate it instead" wording | `DataIntegrityInvariantsTest` (12, incl. `test_a_referenced_structure_is_refused_deletion_instead_of_failing_or_cascading`) | UNIT | VERIFIED |
| H-013 | Data integrity | Twelve invariants checked across request validation, service guard and DB constraint (duplicate assignment, invalid role, rep with clinical access, evaluation pairing, duplicate placement/roster, orphan evidence, transfer transitions, invalid status, locked edits, two reports per week) | Documented matrix in `PRE_SERVER_HARDENING_REPORT.md`; tests added | `DataIntegrityInvariantsTest` | UNIT | VERIFIED |
| H-014 | Data integrity | `reports.assignment_id` foreign key is ON DELETE CASCADE (a raw hard delete of an assignment would delete its reports; no application path does this) | Migration to `restrictOnDelete` recommended (must re-emit the SQLite CHECK and triggers); not applied in this pass | `DataIntegrityInvariantsTest::test_a_report_cannot_exist_without_an_assignment` documents it | n/a | ACCEPTED-MONITOR |
| H-015 | Data integrity | No DB-level check for `author <> subject` on evaluations or `locked => locked_at NOT NULL` on reports (both unreachable through the API) | Trigger extensions recommended; not applied | documented in the same test file | n/a | ACCEPTED-MONITOR |
| H-016 | Audit integrity | Self-service password change and reset-link reset wrote no audit row | `change_password` rows (actor = account holder, `method`, only the `password_change_required` flag before/after; never the password or token) | `AuditIntegrityTest::test_self_service_password_changes_are_audited_without_the_password_or_token` | UNIT | VERIFIED |
| H-017 | Audit integrity | Report comments (create, delete) had no trail | `comment` / `delete` rows on `report_comment` in the same transaction | `AuditIntegrityTest::test_report_comments_are_audited_on_create_and_delete` | UNIT | VERIFIED |
| H-018 | Audit integrity | Access-request approval recorded the status change but not the activation and the assignments it created | `review` rows carry `userActive` before/after and `grantedAssignments` | `AuditIntegrityTest::test_access_request_decisions_record_activation_and_granted_assignments` | UNIT | VERIFIED |
| H-019 | Audit integrity | Audit policies denied update/delete only by omission | `AdminAuditLogPolicy`, `AuditLogPolicy`, `ReportStatusHistoryPolicy` deny `update`, `delete`, `forceDelete` explicitly; no mutation route exists; retention prune is the single designed deletion path (disabled by default) | `AuditIntegrityTest` (16: 20 method/URI mutation attempts × 3 roles all 404/405 with byte-identical tables; policy denials; retention 0 = nothing pruned; non-admins cannot read) | UNIT | VERIFIED |
| H-020 | Audit integrity | Append-only at the database level (triggers or a privilege split) | SQL for BEFORE UPDATE/DELETE triggers with a prune escape hatch written up for the host's first-install step; not applied (not migration-managed) | n/a | n/a | ACCEPTED-MONITOR |
| H-021 | Audit integrity | Roster month save audits counts only; approved account requests have no sibling `create user` row; bulk import has no "import performed" row | Recommended additions listed in the report; not implemented | n/a | n/a | ACCEPTED-MONITOR |
| H-022 | Frontend failure | Autosave re-armed itself 1.4 s after every server error: a failing server was hammered with one error toast per attempt | The refused values' signature suppresses autosave until the user edits or explicitly saves | `failure-recovery.spec.ts` (nurse report form: every status keeps the value, re-enables, retries once) | LOCAL private stack (2026-09-07) | VERIFIED |
| H-023 | Frontend failure | Lock/unlock buttons carried no pending state: a double-click sent the request twice | One lock/unlock in flight at a time | `failure-recovery.spec.ts` (lock and unlock) | LOCAL private stack | VERIFIED |
| H-024 | Frontend failure | Restoring a cleared inbox larger than 50 rows was refused (422, endpoint `max:50`) while the banner said it could still be restored | Restore is sent in batches of 50 | `failure-recovery.spec.ts` (notifications) | LOCAL private stack | VERIFIED |
| H-025 | Frontend failure | Sign-in told the user to re-check the password on a throttled address, an inactive account, an outage or a timeout | Non-credential failures show the server's message; credential failures keep the generic wording (no enumeration) | `failure-recovery.spec.ts` (login: HTTP failures, dropped connection, hung login 30 s) | LOCAL private stack | VERIFIED |
| H-026 | Frontend failure | Inbox bulk actions could be sent twice on a double-click | One inbox action in flight at a time | `failure-recovery.spec.ts` (notifications) | LOCAL private stack | VERIFIED |
| H-027 | Frontend failure | Matrix of 400/401/403/409/422/429/500/502/503/timeout/disconnect across login, password change, report save/submit/lock, role change, settings, evaluation, action item and evidence, export, notifications, transfer, roster | 17 scenarios pass; no permanent spinner, no false success, no duplicate submission, input retained, 401/419 return to sign-in | `failure-recovery.spec.ts` | LOCAL private stack | VERIFIED |
| H-028 | Capacity | No role-aware load profile; the harness signed in per request and shared no credentials model for parity | `mixed` profile (nurse writes, admin dashboards/boards/audit/exports, academic options/history, rep log), single sign-in per VU, overall percentiles, telemetry (docker stats, MariaDB status, PHP-FPM status, queue) and stage runner under `scripts/capacity/` | `load-test.contract.test.mjs` (4) | PARITY (5 stages) | VERIFIED |
| H-029 | Capacity | Parity result: 200 users at 214 req/s, 0.00 % errors, p95 314 ms; 300 users degrade (p95 779 ms) as the PHP-FPM pool queues | Recorded as the PARITY CAPACITY RESULT in `docs/CAPACITY_TEST_REPORT.md`; host recommendation: size `pm.max_children` from real memory, re-run the stages | n/a | PARITY | VERIFIED |
| H-030 | Capacity | A dashboard read after a write pays a cold analytics build (327 ms window, 1.5 s whole archive) | Accepted; revisit if host p95 passes 1 s at a deadline (serve-stale-while-rebuilding is the fix) | n/a | PARITY | ACCEPTED-MONITOR |
| H-031 | Failure recovery | Queue worker stop, PHP-FPM stop, whole-stack restart, disk threshold and 100/300/1000 ms latency drills | Harness `scripts/chaos/run-failure-drills.sh` + `probe.mjs`; results in `docs/FAILURE_RECOVERY_TEST_REPORT.md` | n/a | PARITY | VERIFIED |
| H-032 | Failure recovery | Evidence upload answered 201 and wrote a metadata row while the file write had failed (local disk `throw => false`, return value ignored): orphan metadata, misleading success | `ActionItemEvidenceController` refuses with `StorageWriteFailedException` (500, plain message) before any row is written | `ActionItemTest::test_evidence_upload_refuses_and_records_nothing_when_storage_cannot_write` | UNIT; PARITY re-run 2026-09-09: 500 with the message, no row, no file; 201 after restore | FIXED, VERIFIED |
| H-033 | Failure recovery | With the database unreachable (dropping packets, not refusing), PHP-FPM workers hung on connect until the OS timeout; recovery after the database returned took 188 s | `PDO::ATTR_TIMEOUT` from `DB_CONNECT_TIMEOUT` (default 5 s) on the MariaDB connection | config change; `php artisan test` (config loads) | PARITY re-run 2026-09-09: recovery 36 s (was 188 s), data intact, 0 failed jobs | FIXED, VERIFIED |
| H-034 | Failure recovery | `/up` stays 200 while the database is down | By design (framework liveness); database health lives in the health snapshot and readiness; runbooks and monitors say so | n/a | PARITY | NOT APPLICABLE |
| H-035 | Failure recovery | The workspace bootstrap issues about 80 database round trips; with 100 ms of network latency per hop it takes 2 to 9 s | Not an issue with the local database on the host; recorded for any future remote-database move | n/a | PARITY | ACCEPTED-MONITOR |
| H-036 | Rollback | A first installation could never succeed: `readlink -f` on the missing `current` link returned a path, `deploy.sh` believed a previous release existed and aborted into its rollback path | `PREVIOUS_RELEASE` only set when the link exists | `V2OperationalConfigurationTest` (deploy ordering) still green; rehearsal `scripts/chaos/rollback-rehearsal.sh` | PARITY-style Linux container: deploy, failed deploy (503 on `/up`), rollback to the previous release in 239 ms, data intact, next deploy succeeds | FIXED, VERIFIED |
| H-037 | Retention | Analytics export files were the only unbounded generated data on disk | `app:prune-operational-data` removes export files and rows older than `EXPORT_RETENTION_DAYS` (default 30, 0 disables); path-confined to `analytics-exports/`; policy template written | `OperationalMaintenanceTest` (2 new) | UNIT | VERIFIED |
| H-038 | Smoke | No production smoke suite | `npm run test:smoke` (`tests/smoke/production-smoke.spec.ts`, 14 API-level checks, read-only by default, variables documented in `docs/PRODUCTION_LAUNCH_CHECKLIST.md`) | the suite itself | PARITY 2026-09-09: 14 of 14 in 13.5 s with all four role accounts, the write check and the release assertion | VERIFIED |
| H-044 | Smoke | The suite signed in once per check (10 sign-ins per run) against the 10/min login limiter, so a retry or a second run answered 429; check 4 only accepted a `_session` cookie name while Laravel derives `<slug>-session` from `APP_NAME` | One sign-in per account reused across checks (fresh only for the sign-out check); cookie name matched as `[-_]session` | the suite on parity | PARITY | FIXED, VERIFIED |
| H-045 | Gate harness | The isolated gate polled Vite on `localhost` (resolved to `::1` first on this host) with a 60 s cap; under load the first request's dependency optimisation exceeded it and the gate never started | Poll `http://127.0.0.1:5173` (the address Vite binds; `baseURL` stays `localhost`) with a 180 s cap | gate boots | LOCAL | FIXED, VERIFIED |
| H-039 | Large data | Behaviour at 4 years of weekly reports on MariaDB unmeasured | Seeded 208 weeks (5,887 reports, 501k cell values) plus the 1-year academic fixture; measured seeding, sizes, cold/warm endpoints, exports, slow log, EXPLAIN, dump and restore | n/a | PARITY: 9 min 48 s seed, 190 MB, user-facing endpoints 30 to 150 ms, whole-archive dashboard 2.8 s cold / 45 ms warm, one-year XLSX export ready in 5 s, dump 27.6 MB in 5 s, restore 22 s, no missing index | VERIFIED |
| H-046 | Large data | `GET /api/analytics/overview` with no dates recomputes the whole archive on every call (2.8 s at four years) and is not cached; no SPA caller (tests and the load harness only) | Recommend caching it under the analytics cache version like the dashboard, or retiring the route; not changed in this pass | `AnalyticsTest` covers the route | PARITY | ACCEPTED-MONITOR |
| H-047 | Large data | The whole-archive export is refused above the 5,000-report ceiling (5,597 reports at four years); a one-year XLSX (1,435 rows) is ready in about 5 s | By design; multi-year extracts are done per year (documented) | `AnalyticsExportTest` | PARITY | NOT APPLICABLE |
| H-040 | Accessibility | Automated and manual accessibility state unknown beyond the gate's critical-only axe check | axe sweep over every route and role at 1280 and 390 px, keyboard-only flows, 200 % zoom, large text, reduced motion, forced colours, touch targets; fixes for legitimate critical/serious findings; `docs/ACCESSIBILITY_AUDIT.md` | `a11y-sweep.spec.ts`, `a11y-keyboard.spec.ts`, `a11y-visual.spec.ts`, gate `accessibility.spec.ts` | LOCAL audit, four runs on 2026-09-09: before 11 critical / 71 serious / 9 moderate / 2 minor violations (1,767 nodes) on 78 page states and 28 of 48 specs passing; after 0 / 0 / 0 / 0 and 48 of 48 (9.2 min) | VERIFIED |
| H-041 | Harness | The Vite proxy target was fixed to :8000, so a second throwaway stack could not run beside the gate | `VITE_DEV_API_TARGET` override in `vite.config.ts` | used by the failure and accessibility runs on 8020/5183 and 8030/5193 | LOCAL | VERIFIED |
| H-042 | Docs | Reliability targets, UAT plan, parity environment, data retention template | `docs/RELIABILITY_TARGETS.md` (labelled proposed, needs approval), `docs/UAT_PLAN.md`, `docs/PARITY_ENVIRONMENT.md`, `docs/DATA_RETENTION_POLICY_TEMPLATE.md` | n/a | n/a | VERIFIED |
| H-043 | Baseline | Interaction-budget spec failed once in the baseline gate (Settings p95 418 ms against 400) while lint/type-check/vitest ran concurrently | Re-run alone: 9 of 9 passed (early Submissions samples 618/949/450 ms recorded, not asserted on the dev server) | `performance.spec.ts` | LOCAL | NOT APPLICABLE (environmental) |
| H-048 | Accessibility | Every sheet (account menu, action item, evaluation detail) dropped keyboard focus to `<body>` when closed with Escape: Radix's modal dialog always focuses its own trigger ref, which is empty because the sheets are opened from plain buttons | `SheetContent` records the element focused when it mounts and focuses it (or a visible control with the same name if the row was re-rendered) on close; the shell and the action-item page also remember their opener | `a11y-keyboard.spec.ts` (account sheet on phone, action-item sheet, evaluation detail sheet: "focus returns to the trigger") | LOCAL audit run 4: three sheet flows pass | FIXED |
| H-049 | Accessibility | Muted-text palette below WCAG AA contrast on 1,626 nodes (2.3 to 4.47 : 1); 90 critical nodes with no accessible name or label (sheet close button, template editor inputs and per-question Settings toggle, import file input); definition-list markup, a keyboard-inaccessible scroll region, heading order, duplicate landmarks, an empty table header | eight text tokens darkened to the lightest AA shade (243 utilities, 55 files); `aria-label`s; `<dl>`; focusable named region; heading levels; landmark labels | `a11y-sweep.spec.ts` (axe, 78 states); gate `accessibility.spec.ts` | LOCAL audit run 4: 0 violations of any impact | FIXED |
| H-050 | Accessibility | Keyboard and visual: no focus indicator on the login fields, the sidebar toggle and the dashboard filter selects; login error and report-form status line not announced; roster and rotation tables without caption or header scope; unnamed focusable chart SVGs (25) and a second unnamed stop in the pie chart; duplicate `<h1>` on the dashboard; a sync indicator animating under reduced motion; touch targets under 44 px (sheet close 32 px, workspace toggle 40 px, day tabs 41 px); the dashboard's "Submission pulse" heading overflowed by 65 px at 640 CSS px (200 % zoom) | rings and outlines; `role="alert"` / `role="status"`; captions and `scope="col"`; chart `title`s and `rootTabIndex={-1}` on the pie; `<h2>`; `motion-safe:`; 44 px targets; heading stacks below `md` | `a11y-keyboard.spec.ts`, `a11y-visual.spec.ts` | LOCAL audit run 4: 48 of 48 | FIXED |
| H-051 | Harness | The audit's own measurement was wrong in three places: the focus-indicator probe blurred and re-focused each element (which reset time inputs' segment cursor into an endless Tab loop) and read rings mid-transition; the 200 % zoom check used CSS `zoom` (media queries unchanged, so the desktop layout was measured at half width); several matchers (strict-mode Save locator, evaluation rows excluded by a digit filter, "Mark all read" searched from inside the list, the report form's action bar preceding the grid, the settings select's text updating one render later) | helper cancels transitions for the snapshot and measures segmented inputs without blurring; zoom emulated as a 640 CSS px viewport at DPR 2; matchers and waits corrected | `tests/e2e/helpers/a11y.ts`, the three audit specs | LOCAL audit run 4 | FIXED |
| H-052 | Gate harness | The final full `npm run test:e2e` failed 33 tests: the failure-recovery spec's password-change scenario rotated (and reverted) the shared nurse's password, and Laravel's `AuthenticateSession` then signed out every other session of that user, including the storageState the rest of the suite reuses; separately, `admin-boundary.spec.ts` pinned the superadmin-only permission delta without the new maintenance-only `system.health` | the scenario rotates a second seeded nurse (`hana.abera`) in a fresh session; the boundary spec's expected delta includes `system.health` | `failure-recovery.spec.ts`, `admin-boundary.spec.ts` | LOCAL gate re-run: 222 passed, 2 skipped, 0 failed (20.1 min) | FIXED |
